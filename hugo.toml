baseURL = 'https://9cli.github.io/myblog/'
languageCode = 'en-us'
title = 'Blog'
theme = 'PaperMod'

[outputs]
    home = ["HTML", "RSS", "JSON"]

[params]
    env = 'production'
    title = 'Blog'
    description = 'We only live once, and we could go at any time.'
    keywords = ['Blog', 'Hugo', 'PaperMod']
    author = '9cli'
    
    # 首页个人模式配置
    [params.profileMode]
        enabled = true
        title = '记录日常'
        subtitle = 'We only live once, and we could go at any time.'

    [params.assets]
        disableHLJS = false

    # 开启代码复制按钮
    ShowCodeCopyButtons = true

[menu]
    [[menu.main]]
        identifier = 'archives'
        name = '归档'
        url = 'archives'
        weight = 10
    [[menu.main]]
        identifier = 'search'
        name = '搜索'
        url = 'search'
        weight = 20
    [[menu.main]]
        identifier = 'tags'
        name = '标签'
        url = 'tags'
        weight = 30
